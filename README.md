# Image Converter Pro

A PyQt6-based desktop application to convert images between multiple formats.

## Requirements
- Python 3.12.x (Recommended)
- Windows 10/11
- pip

## Setup (Windows)

```bash
python -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txt
python image_converter_pro.py
```